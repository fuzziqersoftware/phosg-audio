phosg-audio is a basic C++ wrapper library around OpenAL.

Building & installing:
0. Make sure you have Make, g++ and Python installed.
1. `make`
2. `sudo make install`

Tested on Mac OS and Ubuntu, and probably works on many other flavors of Linux.