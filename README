phosg-audio is a basic C++ wrapper library around OpenAL.

Building & installing on macOS or Linux:
0. Make sure you have CMake, g++ and OpenAL installed.
1. `cmake . && make`
2. `sudo make install`

Should work on macOS, Ubuntu (and probably other Linuxes). Might work on Windows.
